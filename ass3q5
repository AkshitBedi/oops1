#include <iostream>
using namespace std;

class Rectangle {
    int l, b;
public:
    void set(int x, int y) {
        l = x;
        b = y;
    }
    int area() {
        return l * b;
    }
};

int main() {
    int n;
    cin >> n;

    Rectangle r[n];

    for(int i = 0; i < n; i++) {
        int l, b;
        cin >> l >> b;
        r[i].set(l, b);
    }

    for(int i = 0; i < n; i++) {
        cout << r[i].area() << endl;
    }

    return 0;
}
