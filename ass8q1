#include <iostream>
using namespace std;

template <class T>
void swapVal(T &a, T &b){
    T temp = a;
    a = b;
    b = temp;
}

int main(){
    int a,b;
    cin>>a>>b;
    swapVal(a,b);
    cout<<a<<" "<<b;
    return 0;
}
